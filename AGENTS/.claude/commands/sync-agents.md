Sync the AGENTS repo

- Commit changes and push
- Pull new changes down

Resolve conflicts intelligently, be pragmatic. When in doubt, ask me for how to resolve conflicts

Before committing, do an audit of the changes to be committed and flag / short-circuit if anything seems super off

# Path

It will live in iCloud Drive as `AGENTS` dir, or in `~/src/personal/AGENTS`